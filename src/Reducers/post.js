const reducer = (state = [], action) => {
  switch (action.type) {
    case "FATCH_ALL":
      return state;
    case "CREATE":
      return state;
    default:
      break;
  }
};

export default reducer;
